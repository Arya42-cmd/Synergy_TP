## Report Generation

Running the program generates the following files in the `output/` directory:

- `cleaned_students.csv` – Cleaned dataset after preprocessing.
- `summary.json` – Dataset statistics in JSON format.
- `cleaning_report.md` – Markdown report summarizing the cleaning operations.

These files can be used for further analysis and validation.